import sum from "../../Utilities/Utitlities";

const Sunglases = () => {
  const num = 34;
  const num2 = 434;
  const final = sum(num, num2);
  console.log(final);
  return <div>{final}</div>;
};

export default Sunglases;
